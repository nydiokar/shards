# Shard 0 Data Structure

## Directories

- `logs/` - Runtime logs and execution traces
- `parquet/` - Parquet data files for structured storage
- `config/` - Configuration files
- `secrets/` - Encrypted secrets and credentials (gitignored)
